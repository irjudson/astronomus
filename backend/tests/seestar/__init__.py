"""Seestar S50 hardware integration test suite.

Test categories:
  hardware  - requires real telescope (--telescope-host=<ip>)
  mock      - uses minimal mock server (always available)
  playback  - uses recorded session files
  dangerous - commands that would affect hardware; mock-only
"""
